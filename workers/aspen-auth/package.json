{
  "name": "aspen-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — AsPEN member-auth gateway. Magic-link login + private-repo content proxy.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
